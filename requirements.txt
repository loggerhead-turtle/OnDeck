# Web portal + Audio Pi server
Flask>=3.0
requests>=2.31

# YouTube audio import (Audio Pi + cloud)
yt-dlp>=2025.6.1
imageio-ffmpeg>=0.5.1   # bundles ffmpeg binary; no apt-get needed on Render

# Stream Deck control + button rendering (Coach Pi)
streamdeck>=0.9.5
Pillow>=10.0

# Production WSGI server (cloud deploy)
gunicorn>=21.0
